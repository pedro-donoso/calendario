1) no estoy seguro de commitear 

git reset HEAD main.js (unstage) nombre archivo

2) encontramos un error luego de commitear, no confirmamos:
git reset HEAD index.html

hacemos la correccion

3) revertir cambios
git checkout -- index.html


4) recuperar archivo borrado
git checkout style.css

5) borrar archivo desde git bash
git rm foo.txt 

6) agregar defer a script.js para cargar despues de style.css
 <script src="main.js" defer></script>

7) git log (ver todos los commits)
: 
(END)

ctrl + c para volver a la linea de comandos

8) regresar al commit "foo"

git checkout 0123456789 (con 6 primeros digitos funciona)

9) ramas
git branch

10) cambiar a rama principal 
git checkout master

* siempre commitear antes de cambiar de ramas

11) borrar rama

git branch -d fondo-verde (nombre archivo) si no se ha commiteado lanzará error

git branch -D fondo-verde (SOLUCION)

12) fusionar 2 ramas
primero ir a rama definitiva
- git checkout master

- git merge nuevo-titulo (rama que queremos integrar)














